{
  "name": "home-stack",
  "version": "1.0.0",
  "description": "Greenfield Docker Compose stack for a homelab auth gateway. Auth stays at the gateway boundary: private APIs consume trusted identity headers injected by Caddy, oauth2-proxy, or agentgateway.",
  "license": "MIT",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1",
    "dotenv": "^17.4.2",
    "typescript": "^6.0.3"
  }
}
